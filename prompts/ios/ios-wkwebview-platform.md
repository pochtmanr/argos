# iOS WKWebView Platform Prompt

Generated path: `/Users/roman/Developer/iosbrowser/prompts/ios/ios-wkwebview-platform.md`

Build the iOS app after desktop contracts stabilize. iOS must use SwiftUI and `WKWebView`; it cannot use Chromium. Avoid desktop-only claims such as full Chromium extension compatibility or reliable fingerprint spoofing.

Focus areas:

- SwiftUI app shell with NavigationStack and sheets.
- WKWebView navigation, loading state, and page metadata.
- iOS-compatible profile storage strategy.
- Keychain-backed vault references.
- Backend sync, AI permission requests, and subscription state.
