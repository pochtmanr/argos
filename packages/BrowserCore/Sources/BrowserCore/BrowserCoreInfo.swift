import Foundation

/// Lightweight build/version marker for the shared browser engine package.
/// Used by host apps to prove the SPM link resolves.
public struct BrowserCoreInfo {
  public static let version = "0.0.1"
}
